import * as api from "../api.urlshortener";

describe("Fetch shortened url", () => {
    it ("should fetch the processed url", async () => {
        // const url = await api.UrlShortener("https://www.google.com/")
    })
})